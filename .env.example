# Copy to .env and fill in your keys
# Note: this app is client-side only — for personal use, keys go in js/config.js
# See js/config.example.js for the actual config format used by the browser.

GOOGLE_VISION_API_KEY=
ANTHROPIC_API_KEY=
WANIKANI_API_TOKEN=
