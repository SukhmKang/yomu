# Backend only. Restart after editing. Never put these in Vercel.
GOOGLE_VISION_API_KEY=
OPENAI_API_KEY=
OPENAI_MODEL=gpt-5.6-luna
HOST=127.0.0.1
PORT=3000
# Production Vercel origin (no trailing slash), for requests through its /api proxy.
ALLOWED_ORIGINS=
APP_PASSWORD=
